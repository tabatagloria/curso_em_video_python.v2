# Create a script that read an integer number and prints on the screen its sucessor and its predecessor:

number = int(input('Enter a number: '))
sum = number + 1
subtraction = number - 1
print('The entered number is {} its sucessor is {} and its predecessor is {}'.format(number, sum, subtraction))